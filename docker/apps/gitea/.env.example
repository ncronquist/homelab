# Gitea
# No additional environment variables required for basic setup.
# Configuration is done through environment variables in compose.yml
# and through the web UI at https://git.home.ncronquist.com on first run.
